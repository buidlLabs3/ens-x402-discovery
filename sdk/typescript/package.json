{
  "name": "@ens-x402-discovery/sdk-typescript",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript SDK for ENS x402 Discovery",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test ./test/*.test.js",
    "example:basic": "npm run build && node ./examples/basic-usage.js",
    "example:x402": "npm run build && node ./examples/discovery-and-payment.js"
  },
  "dependencies": {
    "ethers": "^6.15.0"
  }
}
